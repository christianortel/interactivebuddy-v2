# Vendor Runtime

This folder contains third-party runtime files required for offline local play.

- `matter.min.js`: Matter.js 0.19.0 physics engine from the npm `matter-js` package.
- `matter-js.LICENSE`: bundled MIT license from the same package.

Keep vendored files version-pinned so local playback works without a network connection.
