# Project Status

## Completed Work

- Added separately selectable clean-room basic tools for Poke, Slap, and Tickle, alongside the existing Open Hand grab/drag/throw loop.
- Wired Poke/Slap/Tickle into the live runtime catalog, typed tool catalog, save migration defaults, old-save force-unlocks, runtime scoring tags, mood reactions, contact particles, and static tool-effect audit metadata.
- Added runtime helper checks for Poke and Slap impulse/score math and extended static production smoke so the built bundle must expose Poke, Slap, Tickle, their free basic catalog state, and their audit scoring tags.
- Updated `INTERACTIVE_BUDDY_PARITY.md`, `docs/content-completion-matrix.md`, `docs/weapon-cosmetics-effects-audit.md`, and `README.md` to reflect the shipped basic-subtool batch while keeping strict 1:1 parity status pending reference/browser tuning.
- Re-verified the basic-subtool batch with `node --check main.js`, `node --check js/content.js`, `node --check src/runtime/toolActionMath.ts`, `node --check src/data/tools.ts`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and scoped `git diff --check`.
- Promoted the default room presentation from the old green/lab-styled Base Lab palette to a plainer gray boxed-room preset named Classic Plain, with subtler room grid rendering, updated fallback room colors, matching CSS defaults, private-pack example defaults, and browser-regression expectations.
- Extended static production smoke to require the Classic Plain default room name and gray palette in the built runtime bundle.
- Re-verified the plain default-room batch with `node --check main.js`, `node --check js/asset-packs.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and scoped `git diff --check`.
- Added private asset-pack `toolTextures` support so selected private packs can map spawned tool/prop cosmetic ids or body labels to local or embedded image overrides while procedural clean-room cosmetics remain the fallback.
- Extended asset-pack sanitization, private/template pack examples, asset-pack documentation, `ASSET_GUIDE.md`, `README.md`, `TEST_PLAN.md`, and `INTERACTIVE_BUDDY_PARITY.md` for private tool texture replacement.
- Extended asset validation so `npm run test:assets` now validates live manifest packs plus the template and private example packs, including optional `toolTextures` image mappings.
- Re-verified the private tool-texture batch with `node --check main.js`, `node --check js/asset-packs.js`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `node --check tests/runtime-helpers.mjs`, `node --check src/runtime/roomPresentation.ts`, `node --check js/content.js`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Added private room/background texture support through room-level `texture` and `textureDataUrl` fields, with `textureFit` controlling canvas background fit and palette colors remaining as fallback.
- Extended room presentation helpers, asset-pack sanitization, private/template pack examples, asset-pack validation, docs, and static smoke coverage for private room texture metadata.
- Re-verified the private room-texture batch with `node --check main.js`, `node --check js/asset-packs.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Added private asset-pack UI theme support through constrained `uiTheme.variables`, letting local packs tune menu/HUD colors, borders, accent colors, and menu gradients without arbitrary CSS injection or bundled copied UI graphics.
- Moved the old-style menu colors onto CSS variables, wired selected asset packs to apply/clear UI theme variables, and updated private/template examples, validation, docs, parity matrix, and static smoke coverage.
- Re-verified the private UI-theme batch with `node --check main.js`, `node --check js/asset-packs.js`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Pivoted the project direction on May 6, 2026 from broad clean-room expansion/migration toward high-fidelity private offline fan-remake parity. Added `INTERACTIVE_BUDDY_PARITY.md` as the primary product tracker, rewrote `TODO_BACKLOG.md` around reference-backed parity lanes, updated `DECISIONS.md` to make fidelity the priority over broad TypeScript migration, and updated `ASSET_GUIDE.md` to treat exact visuals/audio as private local pack inputs rather than placeholder-complete work.
- Added private audio sample override support for asset packs: `audioPacks.<id>.samples` can now map feedback events like `impact`, `explosion`, `shock`, `tickle`, `gift`, `boombox`, `paint`, `unlock`, and `select` to local files or embedded `data:audio/...` URLs with optional gain/playback-rate controls. The feedback engine plays those samples when present and falls back to synthesized Web Audio otherwise.
- Added the ignored private asset lane: the runtime now tries `assets/private/manifest.json` after the built-in pack manifest, Vite copies `assets/private` into `dist/` when present, `.gitignore` keeps real private files out of commits, and committed templates under `assets/private/` document the expected manifest, pack, skin, room, UI, and audio file layout.
- Preserved the existing clean-room Matter.js sandbox runtime with ragdoll buddy physics, shop progression, skins, local saves, settings, synthesized audio, asset packs, tests, and visual baselines.
- Added Vite and TypeScript project tooling: `package.json`, `tsconfig.json`, `vite.config.ts`, and `npm run dev` / `npm run build` scripts.
- Added the requested `src/` project structure with typed modules for game boot, physics tuning, buddy rig, mood system, input velocity, tools, economy, shop, skins, save data, renderer, particles, UI formatting, and catalog data.
- Updated `index.html` to boot through `src/main.ts` while keeping the existing clean-room runtime as the live gameplay engine.
- Configured `npm run dev` to build TypeScript and serve the static Vite preview on port 5173, avoiding this sandbox's blocked on-demand esbuild transform path.
- Added placeholder asset guidance under `src/assets/placeholders/`.
- Added required tracking files: `PROJECT_STATUS.md`, `TODO_BACKLOG.md`, `DECISIONS.md`, `TEST_PLAN.md`, and `ASSET_GUIDE.md`.
- Updated `README.md` with setup, offline build, controls, clean-room rules, feature list, extension notes, and limitations.
- Verified `npm run build`, `npm run test:unit`, and `npm run test:assets`.
- Verified `npm run dev` serves the built game at `http://127.0.0.1:5173/` in a bounded smoke check; the HTML references the generated JS bundle and local `vendor/matter.min.js`.
- Added a clean-room force/environment parity batch to the live runtime:
  - Platform: placeable static ledge with `platform-plank` cosmetic metadata and builder scoring.
  - Bumper: placeable high-bounce static circle with `bumper-ring` cosmetic metadata and bounce scoring.
  - Conveyor Belt: placeable moving belt with `conveyor-belt` cosmetic metadata, animated arrows, and side-drag force.
  - Vacuum: held inward force field with suction scoring and cyan field visuals.
  - Repulsor: held outward radial force with pulse scoring and yellow field visuals.
  - Magnet: held prop-focused pull for heavier loose props with magnetic scoring and pink field visuals.
- Updated unit coverage for the new builder factories and instant-placement classification.
- Updated `docs/content-completion-matrix.md` and `docs/weapon-cosmetics-effects-audit.md` for the new shipped batch.
- Re-verified after the batch with `node --check main.js`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and bounded `npm run dev` HTTP smoke.
- Added a clean-room explosives/projectile parity batch to the live runtime:
  - Firecracker: small timed spark explosive with `firecracker-tube` cosmetic metadata.
  - Mine: low puck explosive with `mine-button` cosmetic metadata.
  - Sticky Bomb: attachable compact explosive with `sticky-bomb` cosmetic metadata and optional local constraint attachment.
  - Cartoon Bomb: large wide-radius timed explosive with `large-cartoon-bomb` cosmetic metadata.
  - Cannonball: heavy aim-and-release projectile with `cannonball-iron` cosmetic metadata.
- Updated unit coverage for the new projectile/explosive factories and instant-placement classification.
- Updated `src/data/tools.ts` IDs to match the live runtime for sticky bomb and cartoon bomb.
- Updated content matrix, audit docs, and backlog for the new shipped batch.
- Re-verified after the second batch with `node --check main.js`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and bounded `npm run dev` HTTP smoke.
- Added the final current backlog live tool batch:
  - Crate: aim-and-release chunky box prop with `crate-cross` cosmetic metadata and blunt/object scoring.
  - Money Drop: nice item with `money-drop` cosmetic metadata, money particles, happy/cash scoring, and gentle buddy bump.
  - Treat: nice item with `treat-cookie` cosmetic metadata, crumb particles, happy/nice scoring, and gentle buddy bump.
- Updated `src/data/tools.ts` nice-item IDs to match the live runtime.
- Updated unit coverage for Crate, Money Drop, and Treat factories plus instant-placement classification.
- Updated `docs/content-completion-matrix.md`, `docs/weapon-cosmetics-effects-audit.md`, and `TODO_BACKLOG.md`.
- Re-verified after the final backlog batch with `node --check main.js`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and bounded `npm run dev` HTTP smoke.
- Added `tests/browser-smoke-cdp.mjs`, a Node-only browser smoke runner that serves `dist/` and drives a local Chromium browser through Chrome DevTools Protocol to cover the newly added tools when local browser process launch is available.
- Aligned `src/data/tools.ts` with the live runtime catalog: all 42 live `js/content.js` tool ids now have matching typed catalog ids, with no typed-only or runtime-only ids.
- Re-verified catalog alignment with a Node id comparison, then reran `node --check main.js`, `npm run test:unit`, and `npm run build`.
- Repaired the CDP smoke harness so declarations load before execution, page load events are registered before navigation, CDP commands have timeouts, WebSocket close/error states reject pending work, and the runner uses a browser-level CDP endpoint with a flattened page session.
- Updated `tests/run-browser-smoke.ps1` to launch browsers through PowerShell instead of Node `spawn`, capture Chrome/Edge stderr/stdout logs, use a fresh per-run profile, prefer a locally downloaded Playwright Chromium/headless-shell runtime when present, and fall back to installed Edge/Chrome.
- Worked around the npm profile-cache permission issue by using a workspace npm cache for package installs; installed the JavaScript Playwright package and manually downloaded/extracted Playwright Chromium and headless-shell binaries into ignored local workspace folders for launch testing.
- Re-verified non-browser checks after the browser harness work with `node --check main.js`, `npm run test:unit`, `npm run test:assets`, and `npm run build`.
- Added first-class external CDP workflow scripts:
  - `npm run browser:launch-cdp` starts a Chromium-compatible browser with remote debugging and prints the attach/stop commands.
  - `npm run test:browser-smoke:external` runs the Node CDP smoke against an already-running browser on the selected debug port.
- Updated README and TEST_PLAN with the external-browser workflow for normal PowerShell sessions or CI environments where Chromium renderer IPC is allowed.
- Re-verified after the external CDP workflow additions with `node --check tests/browser-smoke-cdp.mjs`, `node --check main.js`, `npm run test:unit`, `npm run test:assets`, and `npm run build`.
- Added `tests/resolve-playwright-chromium.mjs` plus `npm run browser:resolve-chromium` so CI and normal shells can set `BUDDY_CHROME_PATH` to Playwright's installed Chromium executable.
- Updated `.github/workflows/browser-regression.yml` for the Vite/Node project shape: Node 22 setup, `npm ci`, `npm run build`, unit/assets/runtime/static smoke checks, Playwright browser installation, CDP browser smoke, and the existing Python browser regression.
- Made `tests/run-browser-smoke.ps1` accept `BUDDY_CHROME_PATH`, Linux/macOS browser candidates, and non-Windows `Start-Process` arguments for CI.
- Re-verified the CI/browser-smoke support changes with `node --check tests/browser-smoke-cdp.mjs`, `node --check tests/resolve-playwright-chromium.mjs`, `npm run test:unit`, `npm run test:runtime`, `npm run test:assets`, `npm run build`, and a post-build `npm run test:static-smoke`.
- Added `tests/dist-static-smoke.mjs` and `npm run test:static-smoke`, a renderer-free production `dist/` smoke check that validates the bundled TypeScript shell, runtime chunk, vendored Matter.js, copied asset-pack manifest, critical DOM ids, full live tool catalog presence, and local static serving.
- Reconfirmed `npm run test:browser-smoke` is still blocked by Chromium renderer IPC/sandbox access denied errors in this environment.
- Re-verified after adding the static smoke with `node --check main.js`, `node --check tests/dist-static-smoke.mjs`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Migrated the live runtime's tool lookup and menu category grouping through `src/runtime/toolCatalog.ts`, keeping the legacy catalog data in place while moving the first runtime helper into TypeScript.
- Re-verified the runtime-helper migration with `node --check main.js`, `npm run build`, `npm run test:static-smoke`, `npm run test:unit`, and `npm run test:assets`.
- Migrated the live runtime's screen-to-world coordinate conversion through `src/runtime/coordinates.ts`, keeping the existing `main.js` wrapper but moving the math into a typed helper.
- Re-verified the coordinate-helper migration with `node --check main.js`, `npm run build`, `node --check tests/dist-static-smoke.mjs`, `npm run test:static-smoke`, `npm run test:unit`, and `npm run test:assets`.
- Added `tests/runtime-helpers.mjs` and `npm run test:runtime` to directly cover the TypeScript runtime helpers through Node type stripping.
- Re-verified the runtime-helper test gate with `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and `npm run test:static-smoke`.
- Migrated live runtime vector clamping and finite mass fallback through `src/runtime/physicsMath.ts`, with direct coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the physics-helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime combo multiplier, anti-grind heat scaling, heat increment, and feedback intensity calculations through `src/runtime/scoringMath.ts`, with direct coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the scoring-helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated the live runtime mood-to-face mapping through `src/runtime/moodPresentation.ts`, with direct fallback and mood-case coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the mood-helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated the live runtime tool metadata label display rule through `src/runtime/toolPresentation.ts`, with direct Rubber Blaster cooldown/burst and fallback coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the tool-presentation migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated the live runtime combo meter fill percentage calculation through `src/runtime/scoringMath.ts`, with direct lower-bound, upper-bound, midpoint, and invalid-window coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the combo-fill migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime HUD cash, XP, and combo label formatting through `src/runtime/hudPresentation.ts`, preserving the current rounded, non-locale cash display behavior with direct coverage in `tests/runtime-helpers.mjs`.
- Re-verified the HUD-presentation migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated the live runtime radial overlay clamping math through `src/runtime/coordinates.ts`, with direct center, lower-bound, upper-bound, and cramped-stage coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the radial-overlay migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated the live runtime canvas fit style calculation through `src/runtime/layout.ts`, with direct wide, tall, and near-ratio coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the canvas-layout migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime FPS value calculation and FPS label formatting through `src/runtime/hudPresentation.ts`, with direct minimum, nominal, enabled, and disabled label coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the FPS-presentation migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime gift cost bounds through `src/runtime/economyMath.ts`, with direct minimum, middle, and maximum cost coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the gift-cost migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime gravity-mode config and fallback normalization through `src/runtime/modeSettings.ts`, with direct mode-order, config, and invalid-mode coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the gravity-mode migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime slow-motion time scale and ceiling body Y-position calculations through `src/runtime/modeSettings.ts`, with direct enabled/disabled coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the slow-mode/ceiling-position migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime rope anchor Y-position calculation through `src/runtime/modeSettings.ts`, with direct ceiling-open and ceiling-closed coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the rope-anchor migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime score heat decay calculation through `src/runtime/scoringMath.ts`, with direct normal-decay, floor-to-zero, and custom-window coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the score-heat-decay migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime shake decay and shake cap calculations through `src/runtime/effectsMath.ts`, with direct floor, cap, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the shake-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime rope anchor X-position clamping through `src/runtime/modeSettings.ts`, with direct center, lower-bound, upper-bound, and custom-margin coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the rope-anchor-X migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime rope length minimum/slack calculation through `src/runtime/modeSettings.ts`, with direct minimum, default-slack, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the rope-length migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime liquid drain threshold and liquid level clamp calculations through `src/runtime/liquidMath.ts`, with direct enabled/disabled drain and lower/upper level coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the liquid-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime explosion burst-count clamp calculation through `src/runtime/effectsMath.ts`, with direct minimum, nominal, maximum, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the explosion-burst-count migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime conveyor direction, rubber burst cap, and rubber cooldown calculations through `src/runtime/toolActionMath.ts`, with direct threshold, cap, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the tool-action-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime collision equivalent-mass and impact burst-count calculations through `src/runtime/physicsMath.ts` and `src/runtime/effectsMath.ts`, with direct normal, cap, and small-mass coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the collision-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime impact score scaling, static-surface multiplier, and impact-score cap calculations through `src/runtime/physicsMath.ts`, with direct default, trampoline, static-floor, and custom-cap coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the impact-score migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime grab-assist frame-scale and correction-magnitude calculations through `src/runtime/physicsMath.ts`, with direct minimum, nominal, maximum, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the grab-assist-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime combo timer and rubber burst window clamped countdown calculations through `src/runtime/timerMath.ts`, with direct normal, zero-floor, and custom-minimum coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the timer-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime toast timer expiry decision through `src/runtime/timerMath.ts`, with direct active, zero, and negative coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the toast-expiry migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime frost, goo, pulse, suction, and star status-effect timer extension/decay calculations through `src/runtime/timerMath.ts`, with direct empty, shorter, longer, and clamped-decay coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the status-effect-timer migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime score reward, XP gain, and airborne challenge record amount calculations through `src/runtime/scoringMath.ts`, with direct minimum, nominal, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the score-reward-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime score-feedback sound routing and haptic pulse pattern selection through `src/runtime/feedbackMapping.ts`, with direct reason, tag, select-intensity, clamp, and no-op coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the feedback-mapping migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime haptic eligibility decision through `src/runtime/feedbackMapping.ts`, with direct settings, vibrate-availability, missing-activation, active, prior-active, and inactive coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the haptic-eligibility migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime projectile hit direction fallback and horizontal spin-sign calculations through `src/runtime/physicsMath.ts`, with direct normalized, fallback, custom-threshold, positive, and negative coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the projectile-direction migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime liquid submersion and liquid angular damping factor calculations through `src/runtime/liquidMath.ts`, with direct dry, partial, full, capped, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the liquid-physics-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime liquid friction selection and wall-recovery velocity clamp calculations through `src/runtime/liquidMath.ts` and `src/runtime/physicsMath.ts`, with direct liquid-type, default, positive-cap, negative-cap, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the liquid-friction/recovery-velocity migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, and a post-build `npm run test:static-smoke`.
- Migrated live runtime liquid buoyancy and drag force calculations through `src/runtime/liquidMath.ts`, with direct default, powered, custom-parameter, positive-drag, and negative-drag coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the liquid-force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime airborne fractional-second bank advancement through `src/runtime/scoringMath.ts`, keeping score and mission side effects in `main.js` while adding direct sub-second, exact-second, and multi-second coverage to `tests/runtime-helpers.mjs`.
- Re-verified the airborne-bank migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime self-righting threshold, angular-velocity adjustment, and near-floor recovery force calculations through `src/runtime/physicsMath.ts`, with direct tilt-threshold, velocity-threshold, negative-tilt, custom-threshold, torque, and force-vector coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the self-righting migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime wall-recovery boundary offset calculation through `src/runtime/physicsMath.ts`, with direct no-op, left, right, top, bottom, and custom-limit coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the wall-recovery offset migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Routed the live runtime Cork Popper projectile hit direction fallback through the existing `src/runtime/physicsMath.ts` direction helper, aligning it with the plunger and star projectile handlers already using the typed helper coverage.
- Re-verified the cork direction-helper wiring with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime Magnet tool's magnetic body label predicate through `src/runtime/toolActionMath.ts`, with direct default-label, non-magnetic, undefined, and custom-label coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the magnetic-label migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime Magnet tool's radius and force magnitude calculations through `src/runtime/toolActionMath.ts`, with direct default-radius, custom-radius, normal-mass, minimum-mass, and custom-parameter coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the magnet-force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime Vacuum and Repulsor tool radius and force magnitude calculations through `src/runtime/toolActionMath.ts`, with direct default-radius, custom-radius, default-force, and custom-coefficient coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the vacuum/repulsor force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime Fan and Black Hole tool radius and force magnitude calculations through `src/runtime/toolActionMath.ts`, with direct default-radius, custom-radius, fan-force, black-hole pull-force, black-hole orbit-force, and custom-coefficient coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the fan/black-hole force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime Conveyor force vector and belt phase advancement calculations through `src/runtime/toolActionMath.ts`, with direct forward, reverse, custom-coefficient, forward-wrap, reverse-remainder, and custom-phase coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the conveyor force/phase migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Heat Cone and Frost Puff cone falloff, radius, force-vector, and Frost damping-scale calculations through `src/runtime/toolActionMath.ts`, with direct shared-falloff, default-radius, custom-radius, force-vector, custom-coefficient, and damping-scale coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the heat/frost elemental-force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Goo Mist and Pulse Beam radius, force-vector, angular-velocity, goo-friction, and pulse-falloff calculations through `src/runtime/toolActionMath.ts`, with direct default-radius, custom-radius, force-vector, custom-coefficient, angular, friction-cap, fallback-friction, pulse-minimum, and custom-falloff coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the goo/pulse elemental-force migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Spark Wand range, idle burst chance, jitter vector, force magnitude, angular jitter, and score calculations through `src/runtime/toolActionMath.ts`, with direct default/custom range, threshold, custom chance, jitter, custom jitter, force, custom force, angular, custom angular, score, and custom-score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the spark-wand helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime explosion default radius, base force, score base, falloff, force magnitude, and score calculations through `src/runtime/effectsMath.ts`, with direct provided/default, falsy-fallback, custom-parameter, falloff, force, score, and custom-score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the explosion-math migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Tesla pulse interval, range, target limit, force magnitude, and score calculations plus Boombox beat interval, range, falloff, side, pulse force, angular velocity, note count, and score calculations through `src/runtime/toolActionMath.ts`, with direct default/custom constant, branch, force-vector, angular, count, and score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the Tesla/Boombox helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime nice-item nudge falloff, side, and force-vector calculations plus Confetti Popper range, lift vector, force magnitude, and score branching through `src/runtime/toolActionMath.ts`, with direct side, falloff, force-vector, range, lift-clamp, custom-lift, confetti-force, and score-branch coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the nudge/confetti helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime cork, plunger, and star projectile hit response impulse magnitude, scaled velocity, damped angular velocity, spin angular velocity, and directional velocity calculations through `src/runtime/physicsMath.ts`, with direct impulse, scaled-velocity, damping, spin, and directional-impulse coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the projectile-response helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Paintball, Foam Dart, Cork Popper, Plunger Shot, Star Shot, and Cannonball launch-direction threshold, power-scaled speed, and signed angular-velocity calculations through `src/runtime/physicsMath.ts`, with direct launch-threshold, custom-threshold, speed, and angular-sign coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the projectile-launch helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Ball, Beach Ball, Bowling Ball, Brick, Crate, Boxing Glove, and Anvil throw radius, clamped drag distance, throw-scale, launch-threshold, and signed angular-velocity calculations through `src/runtime/physicsMath.ts`, with direct distance-clamp, throw-scale, powered-radius, threshold, and spin-sign coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the throwable-prop launch helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Money Drop and Treat random toss velocity calculations through `src/runtime/toolActionMath.ts`, with direct edge, centered, and positive random-input coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the nice-item toss helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Rubber Blaster aim-threshold, pellet-speed, score, burst-count, and cooldown calculations through `src/runtime/physicsMath.ts` and `src/runtime/toolActionMath.ts`, with direct pellet-speed and score coverage added to the existing rubber helper tests in `tests/runtime-helpers.mjs`.
- Re-verified the rubber-blaster helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime elastic rope stiffness and max-rope pruning decision calculations through `src/runtime/modeSettings.ts`, with direct default/custom stiffness and pruning-boundary coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the rope helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime particle life decay, spark/music position advancement, type-specific particle gravity, particle keep/drop, and decal lifetime calculations through `src/runtime/effectsMath.ts`, with direct life, position, gravity, velocity, particle-boundary, and decal-boundary coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the particle update helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime screen-shake random offset calculations through `src/runtime/effectsMath.ts`, with direct edge, centered, and mixed random-input coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the screen-shake helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime airborne spawn-grace gating, floor-clearance eligibility, and per-second score calculations through `src/runtime/scoringMath.ts`, with direct grace-boundary, elapsed-time, clearance, missing-torso, custom-threshold, and score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the airborne scoring helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime self-righting near-floor eligibility plus wall-recovery cooldown gating and next-cooldown calculations through `src/runtime/physicsMath.ts`, with direct floor-boundary, missing-position, custom-clearance, cooldown-boundary, and custom-cooldown coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the self-recovery helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Fan, Black Hole, Vacuum, Repulsor, and Magnet score/cooldown calculations plus Repulsor and Magnet angular-velocity nudges through `src/runtime/toolActionMath.ts`, with direct default/custom score, cooldown, and angular-velocity coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the force-tool score/cooldown helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Heat Cone, Frost Puff, Goo Mist, and Pulse Beam score/cooldown calculations through `src/runtime/toolActionMath.ts`, with direct default/custom score and cooldown coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the elemental score/cooldown helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Conveyor and Liquid score/cooldown calculations through `src/runtime/toolActionMath.ts` and `src/runtime/liquidMath.ts`, with direct default/custom score and cooldown coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the conveyor/liquid score-cooldown helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime nearest-body replacement decisions for dynamic-body, buddy-hit fallback, and nearest-buddy selection through `src/runtime/physicsMath.ts`, with direct closer, tie, and farther coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the nearest-target selection helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime generated explosive radius/base-force calculations plus common explosive trigger-time and arm-score calculations through `src/runtime/effectsMath.ts`, with direct trigger-time and arm-score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the explosive setup helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Tickle impulse magnitude and score calculations through `src/runtime/toolActionMath.ts`, with direct default/custom impulse and score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the tickle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime builder and nice-item placement score constants for Trampoline, Platform, Bumper, Conveyor placement, Gift, Money Drop, Treat, Boombox placement, Tesla Coil, and Elastic Rope through `src/runtime/toolActionMath.ts`, with direct default/custom score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the placement score helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime liquid drain and fill score constants through `src/runtime/liquidMath.ts`, with direct default/custom score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the liquid set-score helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime projectile fire score constants for Paintball, Foam Dart, Cork Popper, Plunger Shot, Star Shot, and Cannonball through `src/runtime/toolActionMath.ts`, with direct default/custom score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the projectile fire score helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime projectile hit score constants for Paintball, Foam Dart, Cork Popper, Plunger Shot, and Star Shot through `src/runtime/toolActionMath.ts`, with direct default/custom score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the projectile hit score helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime power-scaled throw score calculations for Ball, Beach Ball, Bowling Ball, Brick, Crate, Boxing Glove, and Anvil through `src/runtime/toolActionMath.ts`, with direct default/custom score coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the throwable prop score helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime hand flick release scoring and Spark Wand score cooldown constants through `src/runtime/toolActionMath.ts`, with direct default/custom score and cooldown coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the hand flick and Spark Wand cooldown helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime hand grab friction, drag elapsed clamp, drag flick scale, flick-source selection, and hand flick angular-velocity clamp calculations through `src/runtime/physicsMath.ts`, with direct friction, elapsed, scale, selection, and angular-clamp coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the hand drag physics helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Boombox initial beat and lifetime constants through `src/runtime/toolActionMath.ts`, with direct default/custom beat and lifetime coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the Boombox initial-state helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Frost, Goo, and Pulse effect duration constants through `src/runtime/toolActionMath.ts`, with direct default/custom duration coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the elemental duration helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Plunger Shot suction duration through `src/runtime/toolActionMath.ts`, with direct default/custom duration coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the Plunger suction duration helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Vacuum, Repulsor, and Magnet ring particle radius/max-radius/life calculations through `src/runtime/toolActionMath.ts`, with direct default/custom ring-effect coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the force ring-effect helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated runtime save migration and save-payload shaping from `main.js` into `src/runtime/saveState.ts`, preserving the live save/load behavior while adding direct TypeScript helper coverage for default settings, tool/skin arrays, gravity-mode migration, challenge bests, custom asset packs, and Set-to-array serialization.
- Re-verified the save-state migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated runtime skin selection, skin render descriptor, sprite scaling, skin physics profile lookup, and base-physics scaling from `main.js` into `src/runtime/skinRuntime.ts`, preserving the live Matter mutation in `main.js` while adding direct TypeScript coverage for fallback selection, textured skins, head/body line widths, moon-boot physics, robot physics, and default physics fallback.
- Re-verified the skin-runtime migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated shop/progression purchase decisions and shop button state selection into `src/runtime/progressionState.ts`, with the live `js/progression.js` controller now delegating tool purchase, skin purchase/equip, insufficient-cash messaging, and owned/equipped button state to typed helpers.
- Updated `npm run test:unit` to use Node type stripping because the JavaScript progression controller now imports a TypeScript runtime helper.
- Re-verified the progression-state migration with `node --check js/progression.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run build`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated challenge progress formatting, challenge start state, timer countdown, progress cap, reward XP/cash calculation, best-result calculation, and challenge label formatting into `src/runtime/challengeState.ts`, with the live `js/challenges.js` controller now delegating those deterministic challenge state transitions to typed helpers.
- Re-verified the challenge-state migration with `node --check js/challenges.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run build`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated mission coverage rotation, mission initialization, mission progress percentage, mission progress cap, and mission reward XP/cash calculation into `src/runtime/challengeState.ts`, with the live `js/challenges.js` controller now delegating mission selection, mission rendering percentage, and mission completion rewards to typed helpers.
- Re-verified the mission-state migration with `node --check js/challenges.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run build`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Heat Cone, Frost Puff, Goo Mist, and Pulse Beam spark particle descriptor calculations through `src/runtime/toolActionMath.ts`, with direct deterministic random-input coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the elemental particle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated replay/save transfer shaping into `src/runtime/transferState.ts`, with live `js/transfer.js` now delegating replay buffer trimming/seconds, replay strip text, replay/save download names and labels, replay-ready toast text, save snapshot envelope creation, and imported-save extraction to typed helpers.
- Re-verified the transfer-state migration with `node --check js/transfer.js`, `node --check tests/runtime-helpers.mjs`, `node --check src/runtime/transferState.ts`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the shared live runtime burst spark particle descriptor calculation used by `spawnBurst` through `src/runtime/effectsMath.ts`, with direct default/custom random-input coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the shared burst particle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the live runtime confetti burst particle descriptor calculation used by `spawnConfettiBurst` through `src/runtime/effectsMath.ts`, with direct default/custom random-input coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the confetti burst particle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime music-note, money-sparkle, and treat-crumb particle descriptor calculations through `src/runtime/effectsMath.ts`, with `main.js` now passing random inputs into typed helpers from `spawnMusicNotes`, `spawnMoneySparkles`, and `spawnTreatCrumbs` and direct default/custom coverage in `tests/runtime-helpers.mjs`.
- Re-verified the music/money/treat particle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime particle draw alpha and bolt midpoint jitter calculations through `src/runtime/effectsMath.ts`, with `drawParticles` now delegating those deterministic render values while keeping canvas drawing local to `main.js`.
- Re-verified the particle-draw helper migration with `node --check main.js`, `node --check src/runtime/effectsMath.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated repeated live runtime minimum-distance clamp calculations for nice-item nudges, force tools, elemental cones, explosions, and boombox pulses through `src/runtime/physicsMath.ts`, with direct below/equal/above minimum coverage added to `tests/runtime-helpers.mjs`.
- Re-verified the minimum-distance helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room preview presentation helpers into `src/runtime/roomPresentation.ts`, with `main.js` now delegating room motif normalization, room swatch labels/colors, and thumbnail CSS variable fallbacks to typed helpers.
- Re-verified the room-presentation migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated buddy wall-recovery combined-bounds aggregation through `src/runtime/physicsMath.ts`, with `recoverBuddyFromWalls` now delegating min/max body-bound calculation before applying the existing wall recovery offset helper.
- Re-verified the wall-recovery bounds helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated asset/audio pack runtime helpers into `src/runtime/assetPackRuntime.ts`, with `main.js` now delegating asset-pack fallback resolution, audio option labels with linked room names, and selected-audio fallback id selection to typed helpers.
- Re-verified the asset-pack runtime migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime vector angle calculation through `src/runtime/physicsMath.ts`, with projectile body angles, elemental field preview angles, and Plunger Shot impact angle now delegating `Math.atan2` through `getVectorAngle`.
- Re-verified the vector-angle helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated tool UI presentation rules into `src/runtime/toolPresentation.ts`, with `main.js` now delegating locked-tool toast text, shop/menu item labels, radial tool aria labels, and active/locked tool button state calculation to typed helpers.
- Re-verified the tool UI presentation migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated tool-selection mouse-constraint configuration into `src/runtime/toolPresentation.ts`, with `selectTool` now delegating hand/non-hand collision mask, stiffness, damping, body clearing, and wind-stop decisions to typed helpers while keeping Matter mutation in `main.js`.
- Re-verified the mouse-constraint config migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime conveyor active-zone gating through `src/runtime/toolActionMath.ts`, with `updateConveyors` now delegating horizontal and vertical body inclusion bounds to `shouldConveyorAffectBody`.
- Re-verified the conveyor active-zone helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated liquid type fallback and resolution into `src/runtime/liquidMath.ts`, with `main.js` now delegating settings normalization, control fallback, preset repair, and runtime liquid lookup to typed helpers.
- Re-verified the liquid type fallback migration with `node --check main.js`, `node --check src/runtime/liquidMath.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated live runtime Pulse Beam side-distance calculation through `src/runtime/toolActionMath.ts`, with `applyPulseBeam` now delegating perpendicular distance from the beam aim vector to `getPulseBeamSideDistance`.
- Re-verified the Pulse Beam side-distance helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated audio pack fallback and resolution into `src/runtime/assetPackRuntime.ts`, with `main.js` now delegating feedback audio pack lookup, settings normalization, setup control value fallback, and change-handler fallback to typed helpers.
- Re-verified the audio-pack fallback migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated the remaining live runtime challenge-mode fallback call sites through `src/runtime/challengeState.ts`, with `main.js` now delegating save migration mode selection and setup/menu select values to `getChallengeModeId`.
- Re-verified the challenge-mode fallback migration with `node --check main.js`, `node --check src/runtime/challengeState.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated Classic Buddy body-part render geometry through `src/runtime/skinRuntime.ts`, with `drawClassicBuddyPart` now delegating radius fallback, radial-gradient geometry, line width, and highlight ellipse dimensions while keeping canvas drawing local to `main.js`.
- Re-verified the Classic Buddy render-geometry helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated asset-pack id normalization into `src/runtime/assetPackRuntime.ts`, with `main.js` now delegating saved room-pack fallback and setup control value fallback to `getSelectedAssetPackId`.
- Re-verified the asset-pack id normalization migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Hardened the browser-smoke harness by resolving repo-local Playwright Chromium without requiring `PLAYWRIGHT_BROWSERS_PATH`, moving CDP browser profiles/logs to the host temp directory by default, preserving headed launch visibility, and adding explicit external-CDP guidance when the Codex sandbox kills Chromium.
- Re-tested `npm run browser:resolve-chromium` successfully without extra environment variables; `npm run test:browser-smoke` still reaches CDP and then fails with `CDP socket closed` after Chromium exits with the known Windows Mojo access-denied fatal.
- Migrated Classic Buddy face render geometry through `src/runtime/skinRuntime.ts`, with `drawClassicBuddyFace` now delegating dot-eye, x-eye, and mood-specific mouth geometry while keeping canvas drawing local to `main.js`.
- Re-verified the Classic Buddy face-geometry helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated circular cosmetic shine arc geometry through `src/runtime/toolPresentation.ts`, with Large Bomb and Cannonball cosmetic drawing now delegating radius-scaled shine position/radius values while keeping canvas drawing local to `main.js`.
- Re-verified the circular cosmetic arc helper migration with `node --check main.js`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run build`, `npm run test:unit`, `npm run test:assets`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated mode/settings UI presentation rules through `src/runtime/modeSettings.ts`, with `main.js` now delegating ceiling, slow-motion, gravity-mode, FPS toast text, boolean mode aria state, and gravity mode active-button state to typed helpers.
- Re-verified the mode/settings presentation migration with `node --check main.js`, `node --check src/runtime/modeSettings.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated scene preset shaping and stored-preset parsing through `src/runtime/saveState.ts`, with `main.js` now delegating scene preset serialization, missing/invalid/loaded toast text, and malformed localStorage handling to typed helpers.
- Re-verified the scene-preset migration with `node --check main.js`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated imported asset-pack JSON parsing and import toast text through `src/runtime/assetPackRuntime.ts`, with `main.js` now delegating wrapped `{ pack }` extraction, malformed JSON handling, duplicate-pack messaging, import success messaging, and import failure messaging to typed helpers.
- Re-verified the imported asset-pack helper migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated tool-use toast presentation through `src/runtime/toolPresentation.ts`, with `main.js` now delegating Platform, Bumper, Conveyor, Gift cash-gate, Money Drop, Treat, Confetti, Boombox, and Rope action messages to typed helpers while keeping physics mutations local.
- Re-verified the tool-use toast migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated liquid control toast presentation through `src/runtime/liquidMath.ts`, with `main.js` now delegating drain and level-set messages while keeping liquid state changes and scoring local.
- Re-verified the liquid toast migration with `node --check main.js`, `node --check src/runtime/liquidMath.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated selection toast presentation for room asset packs, audio packs, and liquid types through `src/runtime/assetPackRuntime.ts` and `src/runtime/liquidMath.ts`, with `main.js` now delegating those selected-state messages while keeping UI control mutation local.
- Re-verified the selection-toast migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check src/runtime/liquidMath.ts`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated general HUD action toast presentation through `src/runtime/hudPresentation.ts`, with `main.js` now delegating new-buddy and scene-reset messages while keeping scene mutation local.
- Re-verified the HUD action-toast migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated explosive armed-toast presentation through `src/runtime/toolPresentation.ts`, with Firecracker, Grenade, Mine, Sticky Bomb attached/unattached, and Cartoon Bomb arming messages now delegated from `main.js`.
- Re-verified the explosive armed-toast migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated challenge option labels and room thumbnail accessibility labels through `src/runtime/challengeState.ts` and `src/runtime/roomPresentation.ts`, with `main.js` now delegating the Free Play select label and room thumbnail aria-label text to typed helpers.
- Re-verified the challenge/room presentation migration with `node --check main.js`, `node --check src/runtime/challengeState.ts`, `node --check src/runtime/roomPresentation.ts`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated power readout formatting through `src/runtime/hudPresentation.ts`, with both the live slider handler and full HUD refresh now delegating power label text to `formatPowerLabel`.
- Re-verified the power readout migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated toolbar and radial tool button title presentation through `src/runtime/toolPresentation.ts`, with `main.js` now delegating numbered rail tooltips and radial wheel tooltips while keeping button DOM creation local.
- Re-verified the tool title migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated tool rail button markup and radial wheel center label presentation through `src/runtime/toolPresentation.ts`, with `main.js` now delegating the tool-button copy markup and radial center label while keeping element creation, event wiring, and positioning local.
- Re-verified the tool rail markup migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room browser active-state presentation through `src/runtime/roomPresentation.ts`, with `main.js` now delegating room-pack active class and `aria-pressed` state while keeping DOM creation and selection events local.
- Re-verified the room browser button-state migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated selected-tool panel text shaping through `src/runtime/toolPresentation.ts`, with `selectTool` now delegating the HUD tool name/description pair while keeping selection state, Matter mouse-constraint mutation, feedback, and persistence local.
- Re-verified the selected-tool panel helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated core HUD presentation bundling through `src/runtime/hudPresentation.ts`, with `updateHud` now delegating cash, XP, combo label, combo-fill percentage, and power label formatting to a typed helper while keeping DOM writes local.
- Re-verified the HUD core presentation migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated radial unlock-button presentation through `src/runtime/toolPresentation.ts`, with `updateUnlockButtons` now delegating radial active/locked state and aria-label selection while keeping DOM class and attribute writes local.
- Re-verified the radial unlock-button helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated radial tool placement presentation through `src/runtime/toolPresentation.ts`, with `buildRadialWheel` now delegating button angle and transform calculation while keeping element creation, icon text, and event wiring local.
- Re-verified the radial placement helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room preview summary and room application presentation through `src/runtime/roomPresentation.ts`, with `renderRoomPreview` now delegating preview pack id/name selection and `applyRoomPack` delegating background/floor fallbacks while keeping DOM and Matter render mutation local.
- Re-verified the room preview/application helper migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated FPS counter visibility presentation through `src/runtime/hudPresentation.ts`, with `updateFpsCounterVisibility` now delegating visible-state and label calculation while keeping DOM class and text writes local.
- Re-verified the FPS counter presentation helper migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated radial overlay CSS position formatting through `src/runtime/coordinates.ts`, with `showRadialWheel` now delegating left/top string shaping after clamping the overlay coordinates while keeping DOM style writes and wheel-open state local.
- Re-verified the radial overlay CSS helper migration with `node --check main.js`, `node --check src/runtime/coordinates.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated screen-shake transform formatting through `src/runtime/effectsMath.ts`, with `drawOverlayEffects` now delegating the canvas `translate(...)` string while keeping random shake sampling and DOM style mutation local.
- Re-verified the screen-shake transform helper migration with `node --check main.js`, `node --check src/runtime/effectsMath.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated toast presentation through `src/runtime/hudPresentation.ts`, with `toast` now delegating visible-class and timer selection while keeping DOM text/class mutation and runtime timer assignment local.
- Re-verified the toast presentation helper migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated boolean mode-button presentation grouping through `src/runtime/modeSettings.ts`, with `updateModeButtonStates` now delegating ceiling and slow-motion aria state together while keeping DOM attribute writes local.
- Re-verified the boolean mode-button helper migration with `node --check main.js`, `node --check src/runtime/modeSettings.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated asset-pack dropdown option presentation through `src/runtime/assetPackRuntime.ts`, with `buildAssetPackUi` now delegating option value/label shaping while keeping select DOM creation and room-preview refresh local.
- Re-verified the asset-pack option helper migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated audio-pack dropdown option presentation through `src/runtime/assetPackRuntime.ts`, with `buildAudioPackUi` now delegating option value/label shaping while keeping select DOM creation and selected-audio fallback local.
- Re-verified the audio-pack option helper migration with `node --check main.js`, `node --check src/runtime/assetPackRuntime.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated challenge-mode dropdown option presentation through `src/runtime/challengeState.ts`, with `buildMenus` now delegating option value/label shaping while keeping select DOM creation and selected-mode fallback local.
- Re-verified the challenge-mode option helper migration with `node --check main.js`, `node --check src/runtime/challengeState.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated item-menu category header presentation through `src/runtime/toolPresentation.ts`, with `buildMenus` now delegating category header class and label shaping while keeping menu DOM creation local.
- Re-verified the menu category helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room browser button presentation through `src/runtime/roomPresentation.ts`, with `renderRoomPreview` now delegating room browser button class, pack id, label class/text, active state, and aria state while keeping DOM creation and room selection events local.
- Re-verified the room browser button presentation helper migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room swatch presentation through `src/runtime/roomPresentation.ts`, with `renderRoomPreview` now delegating swatch class, background, title, and aria-label shaping while keeping swatch DOM creation local.
- Re-verified the room swatch presentation helper migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room thumbnail presentation through `src/runtime/roomPresentation.ts`, with `createRoomThumbnail` now delegating thumbnail class, motif, aria-label, CSS variable map, and layer class names while keeping DOM creation local.
- Re-verified the room thumbnail presentation helper migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Re-tested the browser automation blocker on May 6, 2026: `npm run browser:resolve-chromium` finds the repo-local Playwright Chromium and `npm run test:static-smoke` passes, but `npm run test:browser-smoke` still opens CDP and then fails with `CDP socket closed` after Chromium logs the known Windows Mojo access-denied fatal. Moving the profile root to the workspace does not change the failure, so the next validation path remains launching CDP from a normal Windows PowerShell outside the Codex sandbox.
- Migrated radial wheel visibility presentation through `src/runtime/toolPresentation.ts`, with `showRadialWheel` and `hideRadialWheel` now delegating the open class and radial-open state transition while keeping DOM class mutation local.
- Re-verified the radial wheel visibility helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated mood HUD presentation through `src/runtime/moodPresentation.ts`, with `updateHud` now delegating the mood label and matching face text while keeping DOM writes local.
- Re-verified the mood HUD presentation helper migration with `node --check main.js`, `node --check src/runtime/moodPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated toolbar and radial base-button presentation through `src/runtime/toolPresentation.ts`, with `buildToolUi` and `buildRadialWheel` now delegating button class names, tool ids, titles, icon text, transforms, and rail markup while keeping DOM creation and event wiring local.
- Re-verified the base tool-button presentation helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated room preview shell presentation through `src/runtime/roomPresentation.ts`, with `renderRoomPreview` now delegating the preview pack id, room name class/text, swatch strip class, and room browser container class while keeping DOM creation and selection events local.
- Re-verified the room preview shell helper migration with `node --check main.js`, `node --check src/runtime/roomPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated toast hide presentation through `src/runtime/hudPresentation.ts`, with the runtime timer-expiry path now delegating the toast visible-class removal to the same typed presentation module that controls toast show state.
- Re-verified the toast hide helper migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated FPS sample presentation through `src/runtime/hudPresentation.ts`, with `updateFpsCounter` now delegating frame accumulation, elapsed-window rollover, sampled FPS value, and live label text while keeping runtime state assignment and DOM writes local.
- Re-verified the FPS sample helper migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated number-key tool selection through `src/runtime/toolCatalog.ts`, with the keydown handler now delegating numeric key parsing and catalog bounds checks before selecting a tool.
- Hardened one cosmetic arc helper assertion in `tests/runtime-helpers.mjs` to use tolerance for binary floating-point output instead of exact object equality.
- Re-verified the number-key selection helper migration with `node --check main.js`, `node --check src/runtime/toolCatalog.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated primary-pointer, touch-pointer, and reset-key input decisions through `src/input/InputManager.ts`, with the live keydown/pointerdown/pointerup handlers now delegating those checks to typed helpers while keeping event side effects local.
- Re-verified the input decision helper migration with `node --check main.js`, `node --check src/input/InputManager.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated power control parsing and label presentation through `src/runtime/hudPresentation.ts`, with initial state and the slider input handler now delegating numeric power extraction and readout text while keeping DOM writes local.
- Re-verified the power control helper migration with `node --check main.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated ball cosmetic shine geometry through `src/runtime/toolPresentation.ts`, with `drawBallCosmetic` now delegating the shine circle center/radius calculation to the shared circular cosmetic arc helper.
- Re-verified the ball cosmetic shine helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated treat cosmetic icing geometry through `src/runtime/toolPresentation.ts`, with `drawTreatCosmetic` now delegating the icing circle center/radius calculation to the shared circular cosmetic arc helper while keeping chip and crumb drawing local.
- Re-verified the treat cosmetic icing helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated sticky bomb cosmetic pad geometry through `src/runtime/toolPresentation.ts`, with `drawStickyBombCosmetic` now delegating the pad circle center/radius calculation to the shared circular cosmetic arc helper while keeping fuse drawing local.
- Re-verified the sticky bomb pad helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated treat chip polar geometry through `src/runtime/toolPresentation.ts`, with `drawTreatCosmetic` now delegating chip x/y placement to `getCosmeticPolarPoint` while keeping the chip loop and canvas drawing local.
- Re-verified the treat chip polar helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated bumper cosmetic bolt geometry through `src/runtime/toolPresentation.ts`, with `drawBumperCosmetic` now delegating radial bolt x/y placement to `getCosmeticPolarPoint` while keeping the bolt loop and canvas drawing local.
- Re-verified the bumper bolt polar helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated mine cosmetic tooth geometry through `src/runtime/toolPresentation.ts`, with `drawMineCosmetic` now delegating each radial tooth's inner/outer endpoints to `getCosmeticPolarSegment` while keeping the tooth loop and canvas strokes local.
- Re-verified the mine tooth polar segment helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Migrated beach ball seam and star vertex polar geometry through `src/runtime/toolPresentation.ts`, with `drawBeachBallCosmetic` and `drawStarCosmetic` now delegating polar endpoint/vertex coordinates to `getCosmeticPolarPoint` while keeping path construction and canvas drawing local.
- Re-verified the beach ball seam/star vertex helper migration with `node --check main.js`, `node --check src/runtime/toolPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, a post-build `npm run test:static-smoke`, and scoped `git diff --check`.
- Expanded private audio parity support so score/tool reasons now flow through as exact sample keys for hand basics, nice tools, elemental tools, explosives, force tools, projectile hits, and builder/environment placements.
- Added synthesized fallback coverage for exact events such as Poke, Slap, Money Drop, Treat, Confetti, Heat, Frost, Goo, Pulse, Firecracker, Mine, Sticky Bomb, Cartoon Bomb, Wind, and Liquid, with generic sample fallback still available for older audio packs.
- Extended asset-pack validation, private-pack examples, asset-pack docs, `ASSET_GUIDE.md`, `README.md`, `TEST_PLAN.md`, `INTERACTIVE_BUDDY_PARITY.md`, and `docs/content-completion-matrix.md` for exact-event private audio samples.
- Re-verified the exact-event audio sample batch with `node --check main.js`, `node --check js/feedback.js`, `node --check js/asset-packs.js`, `node --check src/runtime/feedbackMapping.ts`, `node --check tests/unit-modules.mjs`, `node --check tests/runtime-helpers.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added on-canvas Buddy reaction bubbles that render above the head for non-calm mood reactions, with compact Flash-era text for happy, startled, angry, explosive, elemental, nice-tool, and shock reactions.
- Extended `src/runtime/moodPresentation.ts` with reaction bubble text/geometry helpers, wired the live renderer to draw rounded speech bubbles with tail pointers, and added runtime helper/static smoke coverage.
- Re-verified the reaction-bubble batch with `node --check main.js`, `node --check src/runtime/moodPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Added shop category tabs to the live progression shop: All remains the default, paid tools group by their runtime category, skins have their own tab, and the active tab filters the visible buy/equip cards without changing purchase state.
- Extended typed progression helpers for shop category id normalization, labels, counts, active state, and item filtering, with direct runtime helper checks and unit coverage through the fake DOM progression controller.
- Re-verified the shop-category batch with `node --check js/progression.js`, `node --check src/runtime/progressionState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, and `npm run test:static-smoke`.
- Added a Settings > Reset Progress control that confirms before clearing the versioned local save and saved scene preset, removes imported custom asset/audio/skin packs from the live session, restores default money/tool/skin/settings/challenge state, rebuilds the relevant UI, and writes the clean default save back to localStorage.
- Added reset-progress toast/helper coverage plus static smoke assertions for the new control in the built offline HTML/runtime bundle.
- Re-verified the reset-progress batch with `node --check main.js`, `node --check js/ui-bindings.js`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added a persisted Settings > Volume slider that multiplies the selected audio pack's master gain without changing per-pack tone/sample definitions.
- Extended save migration, feedback gain application, UI bindings, static smoke, unit checks, runtime helper checks, README, TEST_PLAN, content matrix, and parity tracker for the new volume setting.
- Re-verified the volume-settings batch with `node --check main.js`, `node --check js/feedback.js`, `node --check js/ui-bindings.js`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added persisted Settings toggles for Camera Shake and Particles. Camera Shake now blocks new shake and clears the active transform when disabled; Particles now gates spawned particles through `addParticle`, clears active particles when disabled, and skips particle rendering while off.
- Extended save migration, UI bindings, static smoke, unit/runtime helper checks, README, TEST_PLAN, content matrix, and parity tracker for camera-shake and particle settings.
- Re-verified the camera-shake/particle-settings batch with `node --check main.js`, `node --check js/ui-bindings.js`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added a persisted Modes > Debug > Physics Debug toggle that draws Matter body outlines, centers, and constraint lines as an on-canvas overlay without switching the main renderer into wireframe mode.
- Extended save migration, UI bindings, static smoke, unit/runtime helper checks, README, TEST_PLAN, content matrix, and parity tracker for physics-debug settings support.
- Re-verified the physics-debug batch with `node --check main.js`, `node --check js/ui-bindings.js`, `node --check src/runtime/saveState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Split sandbox recovery into separate Reset Buddy, Clear Objects, and Reset Room actions. Reset Buddy respawns only the ragdoll, Clear Objects removes spawned props/ropes/explosives/effect clutter while preserving Buddy, and Reset Room performs the full room reset.
- Extended UI bindings, HUD toast helpers, static smoke, unit/runtime helper checks, README, TEST_PLAN, content matrix, and parity tracker for the separate recovery controls.
- Re-verified the separate recovery-controls batch with `node --check main.js`, `node --check js/ui-bindings.js`, `node --check src/runtime/hudPresentation.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added data-driven skin preview cards to the shop. Skin cards now show a compact color/accent body preview and display private or bundled texture metadata as a textured swatch when a skin provides a texture path/data URL.
- Extended typed progression helpers, the progression controller, CSS, unit/runtime helper checks, static smoke, README, content matrix, and parity tracker for skin shop previews.
- Re-verified the skin-preview shop batch with `node --check js/progression.js`, `node --check src/runtime/progressionState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `node --check tests/dist-static-smoke.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added data-driven shop item state presentation. Shop cards now expose owned/active state through CSS classes, `data-owned`, `data-active`, and `aria-current`, so the equipped skin is visible at the card level instead of only through disabled button text.
- Extended typed progression helpers, the progression controller, CSS, unit/runtime helper checks, static smoke, README, content matrix, and parity tracker for owned/equipped shop-card state.
- Re-verified the shop item-state batch with `node --check js/progression.js`, `node --check src/runtime/progressionState.ts`, `node --check tests/runtime-helpers.mjs`, `node --check tests/unit-modules.mjs`, `npm run test:runtime`, `npm run test:unit`, `npm run test:assets`, `npm run build`, `npm run test:static-smoke`, and `git diff --check`.
- Added mouse-wheel power adjustment on the play canvas. Wheel up/down now steps the same power value used by the slider, clamps to the slider min/max, updates the slider/readout, and respects tools that explicitly opt out of power.
- Extended typed HUD presentation helpers, runtime helper checks, static smoke, README, TEST_PLAN, content matrix, and parity tracker for mouse-wheel power control.
- Re-verified the mouse-wheel power batch with focused syntax/runtime checks and the full non-browser gate.
- Added canvas cursor affordances for Buddy-focused interaction. Hand now reports grab/grabbing over Buddy, Poke/Slap/Tickle report a contact crosshair over Buddy, drag/aim operations keep a crosshair, and the cursor resets on pointer leave.
- Extended typed tool-presentation helpers, runtime helper checks, static smoke, README, TEST_PLAN, content matrix, and parity tracker for cursor-state presentation.
- Re-verified the cursor-affordance batch with focused syntax/runtime checks and the full non-browser gate.

## Remaining Work

- Use `INTERACTIVE_BUDDY_PARITY.md` as the source of truth for 1:1 completion and keep it updated before claiming any fidelity batch is done.
- Capture reference evidence for original menus, shop, tool roster/order, prices, buddy physics/reactions, default room, and sound timing.
- Fill `assets/private/manifest.json` from `assets/private/manifest.example.json` and create the private replacement pack from `assets/private/pack.example.json` as real skins, room/background textures, tool textures, UI theme values, room palettes, and audio samples become available.
- Populate exact-event private audio samples for each reference sound slot, then tune gain/playback timing against reference captures.
- Finish the first reference-backed parity batch by tuning default room proportions and buddy scale/physics against captures; the default room is now plainer and Hand/Poke/Slap/Tickle now exist, but both still need browser/manual feel verification.
- Tune reaction bubble text, placement, fade timing, and trigger selection against reference clips once browser/manual visual verification is available.
- Tune shop category names/order, item ordering, prices, and unlock pacing against reference screenshots once available.
- Add direct browser/manual coverage for Buddy hover/grab cursor states, mouse-wheel power changes, skin shop previews, owned/equipped shop-card state, Reset Buddy, Clear Objects, Reset Room, Settings > Volume, Camera Shake, Particles, Modes > Debug > Physics Debug, and Reset Progress, including settings persistence, imported private pack cleanup, and save-preset removal, once browser automation or a host browser pass is available.
- Continue migrating live gameplay runtime helpers from `main.js` and `js/*.js` into native TypeScript modules only when that directly supports parity, verification, or private asset/audio support.
- Get `npm run test:browser-smoke` passing in an environment where Chromium renderers can stay alive.
- Keep the static dist smoke in the non-browser gate set until real browser automation is available.
- Add direct browser regression coverage for Platform, Bumper, Conveyor, Vacuum, Repulsor, and Magnet after browser automation is restored.
- Add direct browser regression coverage for Firecracker, Mine, Sticky Bomb, Cartoon Bomb, and Cannonball after browser automation is restored.
- Add direct browser regression coverage for Crate, Money Drop, and Treat after browser automation is restored.
- Continue tuning tool pacing, unlock prices, mood variety, and Flash-era UI density through reference-backed regression-tested batches.
- Add direct browser coverage for every new parity-target tool as it ships.

## Known Bugs

- Current TypeScript shell still depends on the legacy clean-room runtime module instead of owning the full simulation directly.
- The live runtime now imports small TypeScript helpers through Vite, but the large simulation loop, input handling, rendering, and tool effects still live in `main.js`.
- Full Python Playwright regression could not run because `playwright` is missing from the local Python runtime, and repo-local `pip install` attempts timed out or hit temp-directory permission issues.
- Browser automation remains blocked by this sandbox's Windows process/IPC restrictions. Installed Edge/Chrome, downloaded Chrome-for-Testing, and downloaded Chrome headless shell all expose or nearly expose CDP, then fail renderer startup with `FATAL:mojo\public\cpp\platform\platform_channel.cc ... Access is denied (0x5)` plus Crashpad/network sandbox access-denied messages. The harness now resolves the repo-local browser path, uses host temp profiles by default, and fails with external-CDP instructions instead of hanging.
- `BUDDY_BROWSER_PROFILE_ROOT=C:\Users\chris\interactive-buddy` still produces the same Mojo/network-sandbox access-denied crash, so this is not just a temp-directory/profile-location issue.
- Codex in-app browser automation is separately blocked until the Node REPL uses a newer Node runtime. The machine has bundled Node `v24.14.0` at `C:\Users\chris\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`, but Node REPL currently resolves global Node `v22.20.0` and requires at least `v22.22.0`; set `NODE_REPL_NODE_PATH` to the bundled Node path and restart Codex to retry Browser Use verification.

## Next Task

- Gather/codify original Interactive Buddy reference evidence into `INTERACTIVE_BUDDY_PARITY.md`, then tune default room proportions, buddy scale/physics, cursor affordance, reaction bubbles, shop category order/prices, skin preview/category/equipped-state flow, mouse-wheel power step feel, reset/settings behavior, volume/shake/particle/debug behavior, and private exact-event audio sample timing against that evidence. In parallel, run `powershell -ExecutionPolicy Bypass -File ./tests/launch-cdp-browser.ps1 -DebugPort 9333 -Headed` from a normal Windows PowerShell and then run `npm run test:browser-smoke:external` from the repo to confirm whether host-launched CDP clears the sandbox blocker and allows direct browser coverage for Poke, Slap, Tickle, Classic Plain, cursor states, reaction bubbles, mouse-wheel power, shop category tabs, skin previews, owned/equipped shop-card state, Reset Buddy, Clear Objects, Reset Room, Volume, Camera Shake, Particles, Physics Debug, Reset Progress, private room textures, private tool texture rendering, private UI themes, and exact-event audio hooks.

## Current 1:1 Completion Status

Strict 1:1 completion percentage is not numerically claimable until the reference audit in `INTERACTIVE_BUDDY_PARITY.md` is filled in. The previous 87% figure described the older clean-room feature-completion target and is no longer valid for a strict 1:1 Interactive Buddy parity goal. This batch improves shop skin inspectability, equipped-state clarity, and recovery/settings completeness, but strict visual/feature/audio parity is still not claimable without reference captures, private exact asset/audio inputs where desired, and browser/manual feel verification.
